name: Get containers

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
    get-containers:
        runs-on: ubuntu-latest
        steps:
        - name: Get containers
          uses: Netcracker/qubership-hub-test/actions/container-get@main
